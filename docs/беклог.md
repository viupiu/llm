# Беклог

- [ ] Разобрать, какие узлы в языке ДОС действительно требуют `"parent"` (не `null`). Якорь (`%that_anchor`) — это не родительский узел. Определить критерии, когда node принадлежит родительскому узлу, и зашить логику в `generate_9_PACKAGER__STAGING.py`.
- [ ] **Добавить в `docs/reference/COMMON_DICTIONARIES.md` больше готовых словарей** — собрать эталонные словари из успешных ботов (архивы в `archives/examples/`) и расширить реестр: приветствия/прощания, эмоции, благодарность, раздражение, запрос повторить, работа с номерами телефона/почт, валюты и суммы, и т.д.
